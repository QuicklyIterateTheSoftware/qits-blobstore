# Artifacts config defaults, shipped from this library jar (docs/epics/qits-artifacts/). Library
# jars ship defaults as META-INF/microprofile-config.properties (ordinal 100) — a jar's
# application.properties is ignored — read by whichever app pulls the jar in (the `auth/*` modules
# ship their config the same way). The consuming app / env overrides any of these.
#
# Artifacts owns its OWN named datasource + persistence unit + Flyway lineage: a separate
# file-based H2 under ~/.qits/data/artifacts, never sharing qits' default DB or its migration
# history, so the future split into a standalone service moves files, not data. Tests override the
# url to in-memory H2 (see src/test/resources/application.properties in this and the service module).
#
# EMBEDDED ONLY — no AUTO_SERVER. The url carried `;AUTO_SERVER=TRUE` while this was a library
# inside the monolith, where a second JVM (a dev tool, a second app) opening the same file was a
# plausible thing to want. It is not survivable here: AUTO_SERVER makes the first opener start
# H2's TCP server, which loads org.h2.server.TcpServer by name, and the native binary has no such
# class — so a GraalVM build boots to `ClassNotFoundException: org.h2.server.TcpServer` and dies
# before the HTTP listener. H2's server mode is not supported in a native image at all, so the
# option was never going to survive; and this context is now a single process owning its own
# volume, where a second writer would be a bug rather than a feature — and the automatic listening
# socket is one more unauthenticated port on qits-net. PackagedProcessIT boots the packaged
# artifact against a file-H2 of exactly this shape, which is what catches a regression here.
quarkus.datasource.artifacts.db-kind=h2
quarkus.datasource.artifacts.jdbc.url=jdbc:h2:file:${user.home}/.qits/data/artifacts/h2/artifacts

# A dedicated persistence unit bound to the artifacts datasource, owning only this module's
# entities (the default PU keeps qits' domain entities). packages assigns entity→PU unambiguously.
quarkus.hibernate-orm.artifacts.datasource=artifacts
quarkus.hibernate-orm.artifacts.packages=eu.wohlben.qits.artifacts.entity

# Own Flyway lineage at a distinct classpath location so it never mixes with domain's db/migration.
quarkus.flyway.artifacts.migrate-at-start=true
quarkus.flyway.artifacts.baseline-on-migrate=true
quarkus.flyway.artifacts.locations=classpath:db/artifacts/migration

# Blob content lives on disk (content-addressed, fan-out dirs), decoupled from the metadata rows.
# Relative default matches the qits.repositories.data-dir convention; the app pins it to ~/.qits.
qits.artifacts.blobs-dir=${user.home}/.qits/data/artifacts/blobs

# How long a blob file must sit untouched before the sweep may unlink it. Nothing unlinks anything
# today — garbage collection ships as a dry-run report — but the window is the primitive's, not the
# report's: it closes the race where a client's blob-exists probe answers "have it" for a blob about
# to go, and the manifest referencing it lands after. Seven days covers any in-flight upload session
# by orders of magnitude. Lower it only with a reason; it is the cheap half of the safety.
qits.artifacts.gc.blob-grace-period=P7D

# Where the oci-images GC strategy reads its keep-set from, at plan time, every time. This is the one
# place this service dials another one, and it is deliberate (GC plan ⚖4): the docker keep-set is
# "which shas would a restart pull", and qits-cd is the only thing that knows. It is a plain HTTP GET
# on qits-net, where callers are trusted; the default is cd's network alias, so a deployment needs no
# env of its own. Unreachable is FAIL-CLOSED — the type is reported as failed and the sweep keeps
# every OCI blob — so a wrong value here reclaims nothing rather than deleting a running image. Tests
# point it at a closed port, which is exactly that posture.
qits.artifacts.gc.oci.cd-base-url=http://qits-cd:8080/cd/api
qits.artifacts.gc.oci.cd-timeout=PT10S

# Self-seed the default repository rows at startup: the two CI types (ci-screenshots, ci-videos)
# plus the platform image repository (qits, type oci-images), without which the first docker push
# to a fresh deployment is 404 NAME_UNKNOWN. The gate lives in `service` (fires only when the web
# app boots, NORMAL/dev launch modes, never tests).
qits.artifacts.startup-seed.enabled=true
